in both homophily.ipynb and do, in Table 4 (estimation 2), the unit of observation should be each tiebreak, not the match.
if we found breakpoint conversation data, break point conversation can be the 3rd outcome instead of comeback win (table 5, estimation 3)
heterogeneity analysis:
interact cultural variables (same country, language, ling_prox) with experience (team avg. GS appearance)
interact cultural variables with surface (clay, and grass dummies, use hardcourt dummy as the baseline, omitted category). Be careful with the second, because you already have the tournament FE.
interact culture variables with Hofstede individualism score at team-level (demean)
IC_c = Hofstede individualism score for country c

            For a team with players i,j, team individualism score IC_team=(IC_i+IC_j)/2 

Specification 1: Y_imt=βC_im+θ(C_im×IC_team)+controls +FE+ε_imt

Specification 2: Y_imt=βC_im+βIC_team+θ(C_im×IC_team)+controls +FE+ε_imt